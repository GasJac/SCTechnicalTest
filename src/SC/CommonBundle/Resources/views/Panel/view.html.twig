{% extends "SCCommonBundle:Panel:layout.html.twig" %}

{% block body %}
    <!-- Main content -->
    <section class="content-header">
      <h1 style="text-align: center"><i class="fa fa-student"></i> Student Details</h1>
      <ol class="breadcrumb">
        <li><a href="{{path('student_list')}}"><i class="fa fa-graduation-cap"></i>Students</a></li>
        <li><a href="#">Student Details</a></li>
      </ol>
    </section>
           <section class="invoice" style="margin-bottom: 0; margin-left: 0; margin-right:0; background: #ecf0f5;">
      <!-- title row -->
      <!-- info row -->
      <div class="row invoice-info">
        <!-- /.col -->
        <div class="col-sm-12 col-md-12 invoice-col">

<div id="daff_form" class="well">
      <!-- title row -->
      <div class="row">
        <div class="col-xs-12">
          <h2 class="">
            <i class="fa fa-student-circle-o"></i> {{ student.lastname }}
          </h2>
        </div>
        <!-- /.col -->
      </div>
      <!-- info row -->
      <div class="row invoice-info">
        <!-- /.col -->
        <div class="col-sm-12 invoice-col">
          <table class="table table-student-information">
                    <tbody>
                      <tr>
                        <td><strong>Firstname:</strong></td>
                        <td>{{ student.firstName }}</td>
                      </tr>
                      <tr>
                        <td><strong>Lastname</td></strong>
                        <td>{{ student.lastName }}</a></td>
                      </tr>
                      <tr>
                        <td><strong>Num Etud</strong></td>
                        <td>{{ student.numEtud }}</td>
                      </tr>
                      <tr>
                        <td><strong>Department:</strong></td>
                        <td>{{ student.department.name }}</td>
                      </tr>                                          
                    </tbody>
                  </table>
        </div>
      </div>
       <!-- this row will not appear when printing -->
      <div class="row no-print">
        <div class="col-xs-12">
          <a href="{{path('student_edit', {'id': student.id})}}"><button type="button" class="btn btn-primary pull-left" style="width: 10%">Edit</button></a>
          <a href="{{path('student_list')}}"><button type="button" class="btn btn-primary pull-right" style="width: 10%">Back</button></a>
        </div>
      </div>
    </div>
  </div>
    </section>
{% endblock %}
